{
  "name": "entenarchiv",
  "version": "4.2.0",
  "private": true,
  "type": "module",
  "description": "Private, offlinefähige PWA mit digitalem Regal, Scanner Pro und geführter Zustandsbewertung für LTB-Sammlungen.",
  "scripts": {
    "validate": "node scripts/validate-project.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "build": "node scripts/build-static.mjs",
    "ci": "npm run check && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
