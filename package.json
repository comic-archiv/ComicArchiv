{
  "name": "entenarchiv",
  "version": "4.6.22",
  "private": true,
  "type": "module",
  "description": "Private, offlinefähige PWA mit digitalem Regal, Sammelziele, Share Cards, Sammlungs-DNA, Erscheinungsradar, Scanner Pro und Zustandsassistent für LTB-Sammlungen.",
  "scripts": {
    "validate": "node scripts/validate-project.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "build": "node scripts/build-static.mjs",
    "ci": "npm run check && npm run hardening && npm run calendar:verify && npm run build",
    "calendar:sync": "node scripts/sync-release-calendars.mjs",
    "calendar:verify": "node scripts/verify-calendar-data.mjs",
    "quality:budget": "node scripts/check-quality-budgets.mjs",
    "quality:dead": "node scripts/check-dead-exports.mjs",
    "backup:roundtrip": "node scripts/check-backup-roundtrip.mjs",
    "repo:hygiene": "node scripts/check-repo-hygiene.mjs",
    "hardening": "npm run quality:budget && npm run quality:dead && npm run backup:roundtrip && npm run repo:hygiene"
  },
  "engines": {
    "node": ">=20"
  }
}
